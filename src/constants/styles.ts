import { Text, View, TouchableOpacity, TextInput, Image, ScrollView, FlatList } from 'react-native';

// En NativeWind 4.x, usamos el componente directamente
// No necesitamos styled para envolver componentes de React Native

export {
  Text,
  View, 
  TouchableOpacity, 
  TextInput, 
  Image, 
  ScrollView, 
  FlatList
};
